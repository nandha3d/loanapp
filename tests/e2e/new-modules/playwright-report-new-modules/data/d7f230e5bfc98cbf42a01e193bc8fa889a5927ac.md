# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: 01-chits.spec.ts >> WEB-CHIT chits module >> WEB-CHIT-001 list page loads or shows subscription gate
- Location: tests\e2e\new-modules\01-chits.spec.ts:11:7

# Error details

```
Error: page.goto: net::ERR_CONNECTION_REFUSED at http://localhost:3000/microlending/chits
Call log:
  - navigating to "http://localhost:3000/microlending/chits", waiting until "domcontentloaded"

```

# Test source

```ts
  1  | import { expect, Page } from '@playwright/test';
  2  | 
  3  | export const MODULE = process.env.TEST_MODULE ?? 'microlending';
  4  | export const BASE = `/${MODULE}`;
  5  | 
  6  | export function moduleRoute(path: string) {
  7  |   return `${BASE}${path.startsWith('/') ? path : `/${path}`}`;
  8  | }
  9  | 
  10 | export async function gotoModule(page: Page, path: string) {
> 11 |   const response = await page.goto(moduleRoute(path), { waitUntil: 'domcontentloaded' });
     |                               ^ Error: page.goto: net::ERR_CONNECTION_REFUSED at http://localhost:3000/microlending/chits
  12 |   return response;
  13 | }
  14 | 
  15 | export async function expectAppShell(page: Page) {
  16 |   await expect(page.locator('body')).toBeVisible();
  17 |   await expect(page).not.toHaveURL(/\/login$/);
  18 | }
  19 | 
  20 | export async function expectOneOfText(page: Page, patterns: RegExp[]) {
  21 |   const body = await page.locator('body').innerText();
  22 |   expect(
  23 |     patterns.some((pattern) => pattern.test(body)),
  24 |     `Expected one of ${patterns.map(String).join(', ')} in page body`,
  25 |   ).toBeTruthy();
  26 | }
  27 | 
```